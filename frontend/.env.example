# API base URL — leave empty in dev (Vite proxy handles /api)
# Production example: https://api.example.com/api  or  /api (same-origin via nginx)
VITE_API_BASE_URL=

# Dev proxy target (vite.config.ts reads this)
VITE_API_PROXY_TARGET=http://localhost:3000
